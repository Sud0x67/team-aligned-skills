{
  "name": "team-aligned-skills",
  "version": "0.1.0",
  "private": true,
  "description": "Skill registry for TeamAligned.",
  "type": "module",
  "scripts": {
    "build:catalog": "node scripts/build-catalog.mjs"
  }
}

